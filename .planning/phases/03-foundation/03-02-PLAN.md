---
phase: 03-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - backend/src/middleware/cors.ts
  - backend/src/routes/api.ts
  - backend/src/index.ts
  - backend/openapi.yaml
  - backend/src/routes/docs.ts
  - backend/package.json
  - frontend/app.js
  - backend/tests/cors.test.ts
autonomous: true
requirements:
  - INF-03
  - INF-06
user_setup:
  - service: github-pages
    why: "Frontend hosting that needs CORS access to backend"
    dashboard_config:
      - task: "Verify CORS origin in production matches actual GitHub Pages URL"
        location: "Repository Settings -> Pages -> Custom domain or default github.io URL"
must_haves:
  truths:
    - "Backend responds to preflight OPTIONS requests from chunkstand.github.io"
    - "API endpoints return Access-Control-Allow-Origin: https://chunkstand.github.io header"
    - "OpenAPI specification documents all endpoints with request/response examples"
    - "API documentation is accessible via GET /docs endpoint"
    - "Frontend JavaScript can successfully call backend API without CORS errors"
  artifacts:
    - path: "backend/src/middleware/cors.ts"
      provides: "CORS middleware configured for GitHub Pages origin"
      exports: ["corsMiddleware"]
    - path: "backend/openapi.yaml"
      provides: "Complete OpenAPI 3.0 specification"
      contains: "paths, components.schemas, examples for all endpoints"
    - path: "backend/src/routes/docs.ts"
      provides: "API documentation serving endpoint"
      exports: ["docsRouter"]
    - path: "backend/src/routes/api.ts"
      provides: "Main API routes (game sessions, agents)"
      exports: ["apiRouter"]
    - path: "backend/tests/cors.test.ts"
      provides: "CORS configuration tests"
  key_links:
    - from: "backend/src/index.ts"
      to: "backend/src/middleware/cors.ts"
      via: "CORS middleware mounting before routes"
      pattern: "app.use.*cors"
    - from: "backend/src/index.ts"
      to: "backend/src/routes/docs.ts"
      via: "Documentation route mounting"
      pattern: "app.use.*docsRouter"
    - from: "frontend/app.js"
      to: "backend API"
      via: "fetch with CORS mode"
      pattern: "fetch.*chunkstand.github.io.*cors"
    - from: "backend/openapi.yaml"
      to: "backend/src/routes/api.ts"
      via: "Specification matches implementation"
      pattern: "Endpoint implementations match OpenAPI paths"
---

<objective>
Configure CORS to allow GitHub Pages frontend (chunkstand.github.io) to access the backend API, create comprehensive OpenAPI 3.0 specification documenting all endpoints, and implement basic game session API endpoints.

Purpose: Enable the frontend to communicate with the backend from its GitHub Pages origin, and provide clear API documentation that Phase 7 (Spectator Experience) can reference when building the UI.

Output: CORS-enabled backend that accepts requests from chunkstand.github.io, OpenAPI specification served at /docs, working API endpoints for game session management.
</objective>

<execution_context>
@/Users/chunkstand/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/chunkstand/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-foundation/03-CONTEXT.md
@.planning/phases/03-foundation/03-01-PLAN.md

# Prior plan output will provide:
# - backend/src/index.ts (Express app)
# - backend/src/config/index.ts (configuration)
# - backend/src/lib/prisma.ts (database client)

# Frontend reference:
@/Users/chunkstand/.openclaw/workspace/ai-governance-hot-potato/app.js
@/Users/chunkstand/.openclaw/workspace/ai-governance-hot-potato/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure CORS middleware for GitHub Pages origin</name>
  <files>backend/src/middleware/cors.ts, backend/src/index.ts, backend/src/config/index.ts</files>
  <action>
    Implement CORS middleware per requirement INF-03 to allow GitHub Pages frontend:
    
    1. Update backend/src/config/index.ts to add CORS configuration:
       - Add corsOrigin: string to Config interface (already required in Plan 01)
       - Validate CORS_ORIGIN env var is present (format: https://chunkstand.github.io or http://localhost:3001 for dev)
       - Support multiple origins in development (comma-separated)
    
    2. Create backend/src/middleware/cors.ts:
       - Import cors from 'cors' package
       - Import { config } from '../config'
       - Configure CORS options:
         * origin: config.corsOrigin (or array of origins in dev)
         * methods: ['GET', 'POST', 'PUT', 'DELETE', 'OPTIONS']
         * allowedHeaders: ['Content-Type', 'Authorization']
         * credentials: false (no cookies needed for v1.1)
         * maxAge: 86400 (24 hours for preflight cache)
       - Export configured middleware
    
    3. Update backend/src/index.ts to mount CORS middleware:
       - Import corsMiddleware from './middleware/cors'
       - Add: app.use(corsMiddleware) BEFORE all route handlers
       - Ensure this is before: app.use(express.json())
    
    4. Test CORS headers locally:
       - Start backend on port 3000
       - Start frontend on port 3001 (or use curl with Origin header)
       - Verify response includes: Access-Control-Allow-Origin: https://chunkstand.github.io
    
    IMPORTANT: CORS must be configured BEFORE routes are mounted. The CORS_ORIGIN in production must be exactly "https://chunkstand.github.io" (the GitHub Pages URL from the existing v1.0 deployment).
  </action>
  <verify>
    Test with curl: curl -H "Origin: https://chunkstand.github.io" -H "Access-Control-Request-Method: POST" -X OPTIONS http://localhost:3000/health
    Check: Response includes header Access-Control-Allow-Origin: https://chunkstand.github.io
    Test: curl -H "Origin: https://evil.com" http://localhost:3000/health
    Check: Response should NOT include Access-Control-Allow-Origin for unauthorized origins
  </verify>
  <done>
    CORS middleware is mounted before all routes, allows requests from chunkstand.github.io, handles preflight OPTIONS requests, and rejects unauthorized origins.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create comprehensive OpenAPI 3.0 specification</name>
  <files>backend/openapi.yaml, backend/src/routes/docs.ts</files>
  <action>
    Create complete OpenAPI specification per user decision "Full OpenAPI spec with complete request/response examples" and requirement INF-06:
    
    1. Create backend/openapi.yaml with:
       - OpenAPI version 3.0.3
       - Info: title "AI Governance Arena API", version "1.1.0", description
       - Servers: localhost (dev), staging URL, production URL (placeholders)
       - Tags: Game Sessions, Agents, Health, Documentation
    
    2. Define all endpoints with full specification:
       
       GET /health:
       - Summary: Health check endpoint
       - Responses: 200 (ok with database status), 503 (error)
       - Example response provided
       
       GET /docs:
       - Summary: API documentation (this spec rendered)
       - Responses: 200 with HTML or JSON spec
       
       POST /api/sessions:
       - Summary: Create new game session
       - Request body: { mode: 'demo' | 'ai', agents: AgentInput[] }
       - Response: 201 with GameSession object
       - Example provided for demo mode with 4 agents
       
       GET /api/sessions/{id}:
       - Summary: Get game session by ID
       - Parameters: id (path, UUID)
       - Response: 200 with GameSession including agents array
       
       GET /api/sessions:
       - Summary: List active game sessions
       - Query params: status (optional), limit (default 10)
       - Response: 200 with array of GameSession summaries
       
       POST /api/sessions/{id}/start:
       - Summary: Start a game session
       - Response: 200 with updated session
       
       DELETE /api/sessions/{id}:
       - Summary: End/delete a game session
       - Response: 204 (no content)
    
    3. Define all schemas in components.schemas:
       - GameSession, Agent, Decision, Move (match Prisma schema)
       - AgentInput (for creating agents)
       - Error (unified error format)
       - HealthStatus
    
    4. Include full examples for each schema:
       - Realistic UUIDs, timestamps, enum values
       - Demo agent examples (StrictBot, LenientBot, BalancedBot, GPT-4)
    
    5. Create backend/src/routes/docs.ts:
       - Import swagger-ui-express (add to package.json dependencies)
       - Load openapi.yaml using js-yaml
       - Serve at GET /docs with Swagger UI HTML
       - Also provide GET /openapi.json for raw spec
    
    IMPORTANT: OpenAPI spec must match actual implementation in Task 3. Every endpoint in the spec must have a corresponding implementation. Keep examples realistic and complete.
  </action>
  <verify>
    Validate: npx @apidevtools/swagger-cli validate backend/openapi.yaml (or similar validator)
    Check: openapi.yaml includes all 4 tags and at least 7 endpoints
    Check: All schemas have required fields and example values
    Run: Start server, visit http://localhost:3000/docs → Swagger UI loads
    Run: curl http://localhost:3000/openapi.json → returns valid JSON
  </verify>
  <done>
    OpenAPI 3.0 specification documents all endpoints with request/response schemas and examples. Swagger UI serves interactive docs at /docs, raw spec available at /openapi.json.
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement game session API endpoints</name>
  <files>backend/src/routes/api.ts, backend/src/index.ts, backend/src/types/index.ts</files>
  <action>
    Implement REST API endpoints that match the OpenAPI specification:
    
    1. Create backend/src/types/index.ts:
       - Define TypeScript interfaces matching OpenAPI schemas
       - CreateGameSessionInput, CreateAgentInput, etc.
       - Re-export Prisma types where appropriate
    
    2. Create backend/src/routes/api.ts:
       - Import { Router } from 'express'
       - Import { prisma } from '../lib/prisma'
       - Import types from '../types'
       
       Implement endpoints:
       
       POST /api/sessions:
       - Validate request body (mode, agents array)
       - Create GameSession in database
       - Create Agent records for each agent in input
       - Return 201 with full session object including agents
       - Support demo mode with pre-configured agent types
       
       GET /api/sessions/:id:
       - Validate UUID format
       - Query session with agents included
       - Return 404 if not found
       - Return 200 with session data
       
       GET /api/sessions:
       - Parse query params (status, limit)
       - Query sessions with optional status filter
       - Limit results (default 10, max 100)
       - Return array of session summaries
       
       POST /api/sessions/:id/start:
       - Find session by ID
       - Update status from INIT to AWAITING_ANSWERS
       - Return updated session
       
       DELETE /api/sessions/:id:
       - Find and delete session (cascades to agents via Prisma)
       - Return 204 on success
       - Return 404 if not found
    
    3. Update backend/src/index.ts:
       - Import apiRouter from './routes/api'
       - Add: app.use('/api', apiRouter) AFTER CORS middleware
       - Ensure this is AFTER health router, BEFORE error handler
    
    4. Implement input validation:
       - Use express-validator or manual validation
       - Return 400 with { error: "Validation failed", details: [...] } for invalid input
       - Validate UUID format for IDs
       - Validate enum values (mode: 'demo' | 'ai')
    
    IMPORTANT: Error responses must follow the user decision "Simple JSON error format {error: message}". All endpoints must match the OpenAPI specification created in Task 2.
  </action>
  <verify>
    Test each endpoint with curl:
    
    1. POST /api/sessions with demo mode + 4 agents → returns 201 with session object
    2. GET /api/sessions/{id} with valid UUID → returns 200 with session
    3. GET /api/sessions/{id} with invalid UUID → returns 400
    4. GET /api/sessions?status=INIT → returns filtered list
    5. POST /api/sessions/{id}/start → updates status to AWAITING_ANSWERS
    6. DELETE /api/sessions/{id} → returns 204, session removed
    
    Check: All responses match OpenAPI schema structure
    Check: Error responses use {error: "..."} format
  </verify>
  <done>
    All API endpoints implemented matching OpenAPI specification: POST/GET/DELETE /api/sessions, POST /api/sessions/:id/start. Input validation returns 400 for invalid data. Error responses follow {error: message} format.
  </done>
</task>

<task type="auto">
  <name>Task 4: Create CORS integration test and verify frontend compatibility</name>
  <files>backend/tests/cors.test.ts, frontend/test-cors.html, backend/package.json</files>
  <action>
    Create tests to verify CORS configuration works correctly:
    
    1. Update backend/package.json to add test dependencies:
       - Add to devDependencies: vitest, @types/node, supertest
       - Add script: "test": "vitest run"
       - Add script: "test:watch": "vitest"
    
    2. Create backend/tests/cors.test.ts:
       - Import { describe, it, expect } from 'vitest'
       - Import supertest
       - Test CORS headers on various endpoints:
         * Test authorized origin (chunkstand.github.io) → returns 200 with CORS headers
         * Test unauthorized origin (evil.com) → returns 200 but NO CORS headers (or blocked)
         * Test preflight OPTIONS request → returns 204 with proper headers
         * Test various HTTP methods (GET, POST, DELETE) all have CORS headers
       
    3. Create frontend/test-cors.html for manual testing:
       - Simple HTML file that attempts to call backend API
       - Uses fetch() with mode: 'cors'
       - Displays result (success or CORS error)
       - Include in frontend/ directory (temporary test file)
       
       Example test:
       ```javascript
       fetch('http://localhost:3000/api/sessions', {
         method: 'POST',
         headers: { 'Content-Type': 'application/json' },
         body: JSON.stringify({ mode: 'demo', agents: [...] })
       })
       .then(r => r.json())
       .then(data => console.log('Success:', data))
       .catch(err => console.error('CORS Error:', err));
       ```
    
    4. Add integration instructions to README or comments:
       - How to run CORS tests locally
       - How to test with frontend
       - Expected CORS headers in responses
    
    5. Run tests to verify:
       - npm test should pass
       - All CORS scenarios covered
    
    IMPORTANT: These tests verify that the CORS configuration actually works, not just that middleware is mounted. The frontend test file provides a manual verification path for the actual GitHub Pages deployment.
  </action>
  <verify>
    Run: cd backend && npm test
    Check: All CORS tests pass (authorized origins allowed, unauthorized blocked)
    Manual test: Open frontend/test-cors.html in browser with backend running
    Check: Console shows successful API call, no CORS errors in browser dev tools
    Check: Response headers include Access-Control-Allow-Origin: https://chunkstand.github.io
  </verify>
  <done>
    Automated CORS tests verify configuration works correctly. Frontend test file confirms browser can successfully call backend API without CORS errors.
  </done>
</task>

<task type="auto">
  <name>Task 5: Finalize API documentation and validate spec consistency</name>
  <files>backend/openapi.yaml, backend/README.md</files>
  <action>
    Validate and finalize API documentation:
    
    1. Verify OpenAPI spec matches implementation:
       - Compare each endpoint in openapi.yaml with api.ts implementation
       - Ensure request/response schemas match actual code
       - Update any discrepancies
    
    2. Add missing documentation:
       - Error response schemas (400, 404, 500)
       - Rate limiting headers (if applicable in Phase 5)
       - Authentication requirements (if applicable in Phase 5)
    
    3. Create backend/README.md with:
       - Quick start instructions
       - Environment variable requirements
       - API endpoint summary
       - Link to /docs for full spec
       - Database setup instructions (prisma migrate)
       - Deployment instructions using render.yaml
    
    4. Validate OpenAPI spec is complete:
       - All endpoints have operationId
       - All schemas have descriptions
       - All examples are realistic (valid UUIDs, proper timestamps)
       - Security schemes defined (if needed)
    
    5. Add npm script for spec validation:
       - "validate:spec": "swagger-cli validate openapi.yaml" (optional)
    
    IMPORTANT: The OpenAPI spec is the contract that Phase 7 (Spectator Experience) will use to build the frontend. It must be accurate and complete.
  </action>
  <verify>
    Check: All endpoints in api.ts have corresponding paths in openapi.yaml
    Check: All response schemas match between code and spec
    Check: README.md includes setup and deployment instructions
    Run: Start server, verify /docs renders all endpoints correctly
    Run: curl endpoints and compare responses with spec examples
  </verify>
  <done>
    OpenAPI specification is complete and validated against implementation. README documents setup process. API contract is ready for Phase 7 frontend development.
  </done>
</task>

</tasks>

<verification>
Complete CORS + API verification:

1. **CORS preflight**: `curl -X OPTIONS -H "Origin: https://chunkstand.github.io" -H "Access-Control-Request-Method: POST" http://localhost:3000/api/sessions`
   - Should return 204 with Access-Control-Allow-Origin header

2. **CORS actual request**: `curl -H "Origin: https://chunkstand.github.io" http://localhost:3000/health`
   - Should include Access-Control-Allow-Origin: https://chunkstand.github.io

3. **OpenAPI validation**: Use swagger-cli or similar to validate openapi.yaml syntax

4. **Docs endpoint**: Visit http://localhost:3000/docs in browser
   - Swagger UI should render with all endpoints visible
   - Should be able to execute test requests from the UI

5. **API consistency**: Compare each endpoint's actual response with OpenAPI example
   - POST /api/sessions → response structure matches GameSession schema
   - Error responses → match Error schema

6. **Frontend test**: Open frontend/test-cors.html in browser
   - Should successfully create game session without CORS errors
</verification>

<success_criteria>
- CORS middleware allows requests from https://chunkstand.github.io
- Preflight OPTIONS requests return proper CORS headers
- Unauthorized origins are rejected (no CORS headers)
- OpenAPI specification documents all endpoints with examples
- Swagger UI serves interactive docs at /docs
- API endpoints match OpenAPI specification exactly
- Frontend can successfully call backend API without CORS errors
- All CORS tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-foundation/03-02-SUMMARY.md` documenting:
- CORS configuration (allowed origins, methods, headers)
- OpenAPI specification summary (endpoints, schemas)
- API endpoint overview
- How to access API documentation
- CORS testing approach
- Any deviations from plan or issues encountered
</output>
