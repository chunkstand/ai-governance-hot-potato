---
phase: 04-real-time-core
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [
  "frontend/js/socket-client.js",
  "frontend/js/connection-status.js",
  "index.html",
  "frontend/test-websocket.html"
]
autonomous: true
requirements: [RTC-03, RTC-07]
user_setup: []
must_haves:
  truths:
    - "Frontend can establish WebSocket connection to backend server"
    - "Connection status is visible to user (online/offline indicator)"
    - "Spectator list shows all connected viewers with their connection status"
    - "Status updates in real-time as connections change"
  artifacts:
    - path: "frontend/js/socket-client.js"
      provides: "Socket.io client wrapper for frontend"
      exports: ["SocketClient", "connect", "disconnect", "on", "emit"]
    - path: "frontend/js/connection-status.js"
      provides: "Connection status UI and spectator list"
      exports: ["updateConnectionStatus", "renderSpectatorList"]
    - path: "index.html"
      provides: "Integrated spectator mode with connection status UI"
      contains: "spectator-mode section, connection-status indicator"
  key_links:
    - from: "index.html"
      to: "frontend/js/socket-client.js"
      via: "Script tag loading"
      pattern: "<script src=.*socket-client"
    - from: "frontend/js/socket-client.js"
      to: "backend Socket.io"
      via: "io\\(\\) connection to /spectator namespace"
      pattern: "io\\(.*spectator"
    - from: "frontend/js/connection-status.js"
      to: "frontend/js/socket-client.js"
      via: "Event listeners for connection state changes"
      pattern: "socketClient\\.on\\(\\s*['\"]connect"
---

<objective>
Integrate WebSocket client into the existing frontend HTML/JS, creating a spectator mode with real-time connection status indicators. This plan adds Socket.io-client to the frontend, creates a reusable socket client module, and builds the connection status UI showing online/offline state and spectator list.

Purpose: Spectators need to see connection health and know how many others are watching. This delivers the user-facing WebSocket layer and status visibility.
Output: Frontend can connect to backend WebSocket, connection status visible, spectator list displays connected viewers.
</objective>

<execution_context>
@/Users/chunkstand/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/chunkstand/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-real-time-core/04-CONTEXT.md
@.planning/research/SUMMARY.md
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Socket.io client module for frontend</name>
  <files>frontend/js/socket-client.js, frontend/test-websocket.html</files>
  <action>
    Create the frontend WebSocket client infrastructure:
    
    1. Create frontend/js/socket-client.js:
       - Load Socket.io client from CDN (https://cdn.socket.io/4.8.1/socket.io.min.js)
       - Create SocketClient class/object that:
         * Manages connection to backend (auto-detect dev vs prod URL)
         * Connects to /spectator namespace
         * Handles connection events (connect, disconnect, connect_error)
         * Provides emit() and on() methods for event handling
         * Exposes connection state (isConnected getter)
         - URL detection: Use window.location to determine backend URL
           * Local dev: http://localhost:3000
           * Production: https://ai-arena-backend-staging.onrender.com (or from env)
    
    2. Create frontend/test-websocket.html for manual testing:
       - Simple test page that loads socket-client.js
       - Shows connection status in real-time
       - Has buttons to connect/disconnect manually
       - Displays any received events in a log
    
    The client should handle CORS properly (already configured on backend) and auto-reconnect defaults from Socket.io (custom reconnection logic added in plan 04-04).
  </action>
  <verify>test-websocket.html loads without errors, can establish connection to running backend</verify>
  <done>Socket.io client module created with connection management</done>
</task>

<task type="auto">
  <name>Task 2: Build connection status UI and spectator list display</name>
  <files>frontend/js/connection-status.js, frontend/css/connection-status.css</files>
  <action>
    Create the connection status visualization components:
    
    1. Create frontend/js/connection-status.js:
       - ConnectionStatusManager class that:
         * Renders status indicator (colored dot + text: "Online"/"Offline")
         * Shows current connection state with color coding:
           - Green: Connected
           - Yellow: Connecting/Reconnecting
           - Red: Disconnected
         * Renders spectator list showing:
           - Number of connected spectators
           - List of spectator IDs (or "Spectator #1", "Spectator #2" if anonymous)
           - Their connection status
         * Updates in real-time when connections change
         * Listens to socket-client events to update UI
    
    2. Create frontend/css/connection-status.css:
       - Status indicator styles (dot + text)
       - Spectator list panel styles
       - Color coding for states
       - Mobile-responsive layout
       - Animation for state transitions
    
    Per user decision in 04-CONTEXT.md: Full list showing all connected spectators with connection status. Place at top-right of game board (Claude's discretion for visual style - use colored dot + text pattern).
  </action>
  <verify>CSS compiles properly, connection-status.js exports required functions, UI components render correctly</verify>
  <done>Connection status UI components created with spectator list display</done>
</task>

<task type="auto">
  <name>Task 3: Integrate spectator mode into existing index.html</name>
  <files>index.html</files>
  <action>
    Update the existing index.html to add spectator mode and connection status:
    
    1. Add to <head> section:
       - Link to connection-status.css
       - Socket.io client CDN script (before other scripts)
    
    2. In the game board section (#gameBoard), add:
       - Connection status indicator (top-right corner)
       - Spectator panel showing connected viewers
       - Spectator mode toggle/button (to switch between "Play" and "Spectate" modes)
    
    3. At end of <body>, add script tags:
       - socket-client.js (load first)
       - connection-status.js (load after)
       - Initialize connection status manager when game board loads
    
    4. Integration with existing code:
       - When "Start Game" is clicked, check mode:
         * If spectator mode: Connect to WebSocket and show spectator UI
         * If play mode: Continue with existing v1.0 behavior (for now - Phase 6 will change this)
       - Hide spectator UI when on landing page, show when in game board
    
    Per user decision in 04-CONTEXT.md: Integration location is existing game page (index.html) with spectator mode/view — not a separate page.
  </action>
  <verify>index.html renders without errors, spectator UI elements present, scripts load in correct order</verify>
  <done>Spectator mode integrated into existing game page with connection status visible</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Open frontend/test-websocket.html in browser with backend running
2. Verify connection establishes (green status indicator)
3. Disconnect backend - verify status changes to red "Offline"
4. Reconnect backend - verify status returns to green "Online"
5. Open index.html in browser
6. Verify connection status UI visible in game board section
7. Check spectator panel renders (even if empty initially)
</verification>

<success_criteria>
- Socket.io client connects to backend successfully
- Connection status visible (green/red/yellow indicator with text)
- Spectator list panel renders on game board
- Status updates automatically on connection state changes
- Frontend files load without console errors
- Integration preserves existing v1.0 functionality
</success_criteria>

<output>
After completion, create `.planning/phases/04-real-time-core/04-02-SUMMARY.md`
</output>
