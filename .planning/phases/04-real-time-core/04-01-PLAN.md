---
phase: 04-real-time-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "backend/package.json",
  "backend/src/socket/index.ts",
  "backend/src/socket/namespaces/game.ts",
  "backend/src/socket/namespaces/spectator.ts",
  "backend/src/socket/roomManager.ts",
  "backend/src/index.ts",
  "backend/.env.example"
]
autonomous: true
requirements: [RTC-01, RTC-02]
user_setup: []
must_haves:
  truths:
    - "Socket.io server is running and accessible on the same port as Express"
    - "Multiple namespaces exist for game and spectator separation"
    - "Room management creates isolated game sessions with sequential naming"
    - "Spectator rooms are accessible at /spectator/:gameId namespace"
  artifacts:
    - path: "backend/src/socket/index.ts"
      provides: "Socket.io server initialization and Express integration"
      exports: ["initializeSocketServer", "getIO"]
    - path: "backend/src/socket/namespaces/game.ts"
      provides: "Game namespace for agent communication"
      exports: ["setupGameNamespace"]
    - path: "backend/src/socket/namespaces/spectator.ts"
      provides: "Spectator namespace for viewer connections"
      exports: ["setupSpectatorNamespace"]
    - path: "backend/src/socket/roomManager.ts"
      provides: "Room creation and management with sequential naming"
      exports: ["createGameRoom", "getRoomByGameId", "cleanupRoom"]
  key_links:
    - from: "backend/src/index.ts"
      to: "backend/src/socket/index.ts"
      via: "Socket.io initialization on HTTP server"
      pattern: "initializeSocketServer\\(server\\)"
    - from: "backend/src/socket/index.ts"
      to: "backend/src/socket/namespaces/spectator.ts"
      via: "Namespace setup"
      pattern: "io\\.of\\(\\s*['/\"]spectator"
    - from: "backend/src/socket/roomManager.ts"
      to: "namespaces"
      via: "Room creation with game:XXX format"
      pattern: "game:\\d{3}"
---

<objective>
Establish Socket.io server infrastructure with namespace/room support for game isolation and spectator viewing. This plan installs Socket.io, creates the server initialization layer, sets up multiple namespaces for separation of concerns, and implements room management with sequential naming (game:001, game:002).

Purpose: Without this infrastructure, real-time communication is impossible. This is the foundation that all subsequent WebSocket features depend on.
Output: Socket.io integrated with Express, /game and /spectator namespaces operational, room manager ready for game session isolation.
</objective>

<execution_context>
@/Users/chunkstand/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/chunkstand/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-real-time-core/04-CONTEXT.md
@.planning/research/SUMMARY.md
@backend/package.json
@backend/src/index.ts
@backend/prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Socket.io dependencies and update configuration</name>
  <files>backend/package.json, backend/.env.example, backend/src/config/index.ts</files>
  <action>
    Install Socket.io server (socket.io@^4.8.0) as a dependency. Add SOCKET_CORS_ORIGIN to .env.example (default to CORS_ORIGIN value for consistency). Update backend/src/config/index.ts to include socketCorsOrigin in the Config interface and validate it (optional, can default to corsOrigin). The server will need CORS configuration for Socket.io to allow GitHub Pages frontend connections.
    
    Run npm install in backend directory after updating package.json. Verify installation with npm list socket.io.
  </action>
  <verify>npm list socket.io returns version 4.8.x or higher, config has socketCorsOrigin field</verify>
  <done>Socket.io installed, configuration ready for WebSocket CORS</done>
</task>

<task type="auto">
  <name>Task 2: Create Socket.io server initialization and namespace setup</name>
  <files>backend/src/socket/index.ts, backend/src/socket/namespaces/game.ts, backend/src/socket/namespaces/spectator.ts</files>
  <action>
    Create the Socket.io server structure:
    
    1. backend/src/socket/index.ts - Main initialization:
       - Import Server from 'socket.io'
       - Create initializeSocketServer(httpServer) function that:
         * Attaches Socket.io to the HTTP server with CORS configuration
         * Sets up global connection logging
         * Calls setupGameNamespace(io) and setupSpectatorNamespace(io)
         * Returns io instance for access elsewhere
       - Create getIO() function to retrieve io instance
       - Export both functions
    
    2. backend/src/socket/namespaces/game.ts - Game namespace:
       - Setup function for /game namespace
       - Handle agent connections (Phase 6 will add actual game logic)
       - For now: log connections, handle disconnect with cleanup
    
    3. backend/src/socket/namespaces/spectator.ts - Spectator namespace:
       - Setup function for /spectator namespace
       - Handle spectator connections
       - Join spectators to appropriate rooms based on gameId parameter
       - Log connections/disconnections
    
    Use sequential room naming pattern: game:001, game:002, etc. (3-digit zero-padded)
  </action>
  <verify>TypeScript compilation passes (npm run typecheck), files created at correct paths</verify>
  <done>Socket.io server structure created with game and spectator namespaces</done>
</task>

<task type="auto">
  <name>Task 3: Create room manager and integrate with Express server</name>
  <files>backend/src/socket/roomManager.ts, backend/src/index.ts</files>
  <action>
    1. Create backend/src/socket/roomManager.ts:
       - Track active rooms in a Map or object
       - createGameRoom(gameId: string) → creates room with format "game:XXX" where XXX is sequential (001, 002...)
       - getRoomByGameId(gameId: string) → returns room name or null
       - cleanupRoom(roomName: string) → removes room from tracking when empty
       - getActiveRoomCount() → returns number of active rooms
       - getRoomSpectatorCount(roomName: string) → returns number of connected spectators
    
    2. Update backend/src/index.ts:
       - Import initializeSocketServer from './socket/index'
       - After creating HTTP server with app.listen(), pass server to initializeSocketServer(server)
       - Keep graceful shutdown handler (it will close both HTTP and WebSocket connections)
    
    3. Add socket.io types if needed (install @types/socket.io or check if included)
    
    The room manager uses sequential naming (game:001, game:002) per user decision in 04-CONTEXT.md. Auto-cleanup happens when last person leaves (this is built into Socket.io, but we track it for monitoring).
  </action>
  <verify>npm run build succeeds, server starts with "Socket.io initialized" message, no TypeScript errors</verify>
  <done>Socket.io integrated with Express, room manager operational, namespaces ready</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Run `cd backend && npm run build` - should compile without errors
2. Start server with `npm run dev` - should show Socket.io initialization message
3. Verify Socket.io is accessible: Server logs should show io initialized
4. Check that /game and /spectator namespaces are created
5. Confirm room manager can create sequential rooms (game:001, game:002)
</verification>

<success_criteria>
- Socket.io server running on same port as Express (3000 default)
- Two namespaces operational: /game and /spectator
- Room manager creates sequential rooms (game:XXX format)
- TypeScript compilation passes
- Server starts without errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-real-time-core/04-01-SUMMARY.md`
</output>
