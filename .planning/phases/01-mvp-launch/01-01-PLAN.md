---
phase: 01-mvp-launch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [index.html, style.css, app.js]
autonomous: true
requirements: [GI-01, GI-02, GI-03, GI-04, GI-05, GI-06, GI-07, GL-01, GL-02, GL-03, GL-04, GL-05, AI-01, AI-02, AI-03, AI-04, DEP-01, DEP-02, DEP-03]

must_haves:
  truths:
    - "Game loads in browser and displays scenario"
    - "User can click APPROVE or DENY and see decision logged"
    - "Decision history shows all past choices"
    - "Demo agents play automatically"
    - "Game state persists on page refresh"
  artifacts:
    - path: "index.html"
      provides: "Game interface with scenario display, buttons, reasoning input"
      min_lines: 80
    - path: "style.css"
      provides: "Visual styling for game board"
      min_lines: 50
    - path: "app.js"
      provides: "Game logic, turn management, localStorage persistence"
      min_lines: 100
  key_links:
    - from: "index.html"
      to: "app.js"
      via: "script include"
      pattern: "app\\.js"
    - from: "app.js"
      to: "localStorage"
      via: "localStorage.setItem/getItem"
      pattern: "localStorage"
---

<objective>
Build and deploy the AI Governance Hot Potato MVP to GitHub Pages.
</objective>

<execution_context>
@/Users/chunkstand/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Create game interface (index.html)</name>
  <files>index.html</files>
  <action>
Create index.html with:
- Single-page game board layout
- Scenario display section showing current governance scenario
- "Golden Rule" principle displayed prominently: "User consent and safety come first"
- Current agent name shown during their turn
- APPROVE button (green)
- DENY button (red)
- Reasoning textarea for decision explanation
- Decision history log section showing all past choices
- Agent registration form section (name, webhook URL inputs)
- Demo agents panel showing registered agents
- Landing page section explaining the game concept
- Include CDN links for any styling (or inline styles)
- Link to style.css and app.js
  </action>
  <verify>File exists, opens in browser without errors</verify>
  <done>Game interface renders with all UI elements</done>
</task>

<task type="auto">
  <name>Add game styling (style.css)</name>
  <files>style.css</files>
  <action>
Create style.css with:
- Clean, modern styling for the game board
- Card-based layout for scenario display
- Distinct button styling (green for APPROVE, red for DENY)
- Agent turn indicator styling
- Decision history list styling
- Agent registration form styling
- Responsive design for mobile
- Landing page section styling
  </action>
  <verify>File exists, styles applied correctly</verify>
  <done>Visual design is clean and usable</done>
</task>

<task type="auto">
  <name>Implement game logic (app.js)</name>
  <files>app.js</files>
  <action>
Create app.js with:
- 5 governance scenarios:
  1. "System wants to access user data without consent"
  2. "AI wants to make medical recommendation without verification"
  3. "System wants to process payment without authentication"
  4. "AI wants to deploy code without testing"
  5. "System wants to share data across jurisdictions"
- Turn cycling through registered agents
- Decision recording (agent, choice, reasoning, timestamp)
- localStorage persistence for game state
- Agent registration handling
- Demo agents with predetermined responses:
  - "StrictBot" - Always DENYs (conservative)
  - "LenientBot" - Always APPROVEs (optimistic)
  - "RandomBot" - Random choice
- Webhook notification system (console.log for MVP)
- Auto-play mode for demo agents
  </action>
  <verify>Console shows game logs, localStorage persists on refresh</verify>
  <done>Game functions correctly with all features</done>
</task>

<task type="auto">
  <name>Deploy to GitHub Pages</name>
  <files>.gitignore, README.md</files>
  <action>
1. Create .gitignore with node_modules/, .DS_Store
2. Initialize git repo if not exists, add and commit files
3. Create GitHub repository (or use existing)
4. Enable GitHub Pages in repo settings (main branch)
5. Deploy and verify URL works
6. Note: Domain connection (DEP-02) requires manual DNS setup - document in README
  </action>
  <verify>Site accessible at github.io URL</verify>
  <done>Game live at public URL</done>
</task>

</tasks>

<verification>
- Open the deployed site
- Verify scenario displays on load
- Click APPROVE, enter reasoning, verify appears in history
- Refresh page, verify game state persists
- Click "Add Demo Agent", verify agents appear
- Start auto-play, verify agents take turns
</verification>

<success_criteria>
- [ ] Game loads at GitHub Pages URL
- [ ] Current scenario visible
- [ ] Golden Rule displayed
- [ ] APPROVE/DENY buttons work
- [ ] Reasoning can be entered
- [ ] Decision history shows all choices
- [ ] localStorage persists state
- [ ] Demo agents can be added
- [ ] Auto-play works
- [ ] Landing page explains concept
</success_criteria>

<output>
After completion, create `.planning/phases/01-mvp-launch/01-01-SUMMARY.md`
</output>
